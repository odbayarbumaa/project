# Although this project is fully functionable, it is still being worked on. 
# If there is a segmentation fault, please try running again. It works most of the time. 
# Currently working looking at all the pointers, and removing all seg. faults. 


In order to run this program,

1) Execute the command "rm -f multi-lookup" which will clean any previously stored data. 

2) Execute the command "gcc -Wall -Wextra -pthread multi-lookup.c -o multi-lookup" which will compile the program 

3) Following the synopsis "multi-lookup <# requester> <# resolver> <requester log> <resolver log> [ <data file> ...]",

   Execute the following command line: 

"./multi-lookup 4 3 serviced.txt results.txt input/names1.txt input/names2.txt input/names3.txt input/names4.txt input/names5.txt"
